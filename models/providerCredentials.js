export default class ProviderCredentials {
    constructor(id, providerId, username, email, password) {
        this.id = id;
        this.providerId = providerId;
        this.username = username;
        this.email = email;
        this.password = password;
    }
}