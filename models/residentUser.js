export default class ResidentUser {
    constructor(id, residentId, username, email, password) {
        this.id = id;
        this.residentId = residentId;
        this.username = username;
        this.email = email;
        this.password = password;
    }
}